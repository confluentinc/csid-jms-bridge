#
# Copyright 2020 Confluent Inc.
#
bridge {
  id = "test-bridge"
  kafka {
    "bootstrap.servers" = "localhost:9092"
  }

  routing {

    routes += {
      name = "my-route"
      from.include = "address://my-jms-topic"
      to.topic = "my-kafka-topic"
    }
  }
}
