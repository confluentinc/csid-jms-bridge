#
# Copyright 2020 Confluent Inc.
#
bridge {
  id = "test-bridge"


  journals {
    topic {
      replication = 1
    }
  }

  kafka {
    "bootstrap.servers" = "replaceme:9092"
    "linger.ms" = 10
  }

  streams {
     "commit.interval.ms" = 1000
     "cache.max.bytes.buffering" = 0
  }
}
