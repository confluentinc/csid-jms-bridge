<!--
  ~ Copyright 2020 Confluent Inc.
  -->

<assembly xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xmlns="http://maven.apache.org/plugins/maven-assembly-plugin/assembly/1.1.2"
  xsi:schemaLocation="http://maven.apache.org/plugins/maven-assembly-plugin/assembly/1.1.2
          http://maven.apache.org/xsd/assembly-1.1.2.xsd">
  <!-- Assembles a package that can run the ksql cli given dependency packages. This is used to
       construct packages OS packages where underlying libraries can be shared
       -->
  <dependencySets>
    <dependencySet>
      <excludes>
        <exclude>io.confluent:common-*</exclude>
        <exclude>com.google.guava:guava</exclude>
      </excludes>
      <outputDirectory>share/java/jms-bridge</outputDirectory>
      <!-- Warning: Enabling useTransitiveFiltering is potentially unsafe,
           cf. https://github.com/miguno/maven-assembly-transitive-deps-woes
           and https://issues.apache.org/jira/browse/MASSEMBLY-504.  If
           dependencies happen to be missing in the assembly you may want to
           disable transitive filtering. -->
      <useProjectArtifact>true</useProjectArtifact>
      <useTransitiveFiltering>true</useTransitiveFiltering>
    </dependencySet>
  </dependencySets>
  <fileSets>
    <fileSet>
      <directory>${project.parent.basedir}</directory>
      <includes>
        <include>version.txt</include>
        <include>COPYRIGHT*</include>
      </includes>
      <outputDirectory>share/doc/jms-bridge/</outputDirectory>
    </fileSet>
    <fileSet>
      <directory>${project.parent.basedir}</directory>
      <includes>
        <include>bin/*</include>
      </includes>
      <outputDirectory></outputDirectory>
    </fileSet>
    <fileSet>
      <directory>${project.parent.basedir}/config</directory>
      <includes>
        <include>*</include>
      </includes>
      <outputDirectory>etc/jms-bridge</outputDirectory>
    </fileSet>
  </fileSets>
  <formats>
    <format>dir</format>
  </formats>
  <id>package</id>
  <includeBaseDirectory>false</includeBaseDirectory>
</assembly>